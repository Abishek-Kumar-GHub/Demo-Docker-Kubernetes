apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-hello-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-hello
  template:
    metadata:
      labels:
        app: python-hello
    spec:
      containers:
        - name: python-hello
          image: your_dockerhub_username/python-hello:latest
          ports:
            - containerPort: 80
